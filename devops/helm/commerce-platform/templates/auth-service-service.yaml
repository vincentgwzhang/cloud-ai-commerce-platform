{{- if .Values.services.auth.enabled }}
apiVersion: v1
kind: Service
metadata:
  name: auth-service
  namespace: {{ include "commerce-platform.namespace" . }}
  labels:
    {{- include "commerce-platform.serviceLabels" (dict "root" . "appName" "auth-service") | nindent 4 }}
spec:
  type: ClusterIP
  selector:
    {{- include "commerce-platform.selectorLabels" (dict "root" . "appName" "auth-service") | nindent 4 }}
  ports:
    - name: http
      port: {{ .Values.services.auth.servicePort }}
      targetPort: {{ .Values.services.auth.containerPort }}
{{- end }}
