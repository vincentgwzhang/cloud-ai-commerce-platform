{{- if .Values.services.order.enabled }}
apiVersion: apps/v1
kind: Deployment
metadata:
  name: order-service
  namespace: {{ include "commerce-platform.namespace" . }}
  labels:
    {{- include "commerce-platform.serviceLabels" (dict "root" . "appName" "order-service") | nindent 4 }}
spec:
  replicas: {{ .Values.services.order.replicaCount }}
  selector:
    matchLabels:
      {{- include "commerce-platform.selectorLabels" (dict "root" . "appName" "order-service") | nindent 6 }}
  template:
    metadata:
      labels:
        {{- include "commerce-platform.selectorLabels" (dict "root" . "appName" "order-service") | nindent 8 }}
    spec:
      containers:
        - name: order-service
          image: {{ include "commerce-platform.image" .Values.services.order.image }}
          imagePullPolicy: {{ .Values.imagePullPolicy }}
          ports:
            - containerPort: {{ .Values.services.order.containerPort }}
          envFrom:
            - configMapRef:
                name: order-service-config
            - secretRef:
                name: {{ .Values.secrets.dbSecretName }}
          volumeMounts:
            - name: jwt-keys
              mountPath: /app/keys
              readOnly: true
          readinessProbe:
            httpGet:
              path: {{ .Values.services.order.probes.path }}
              port: {{ .Values.services.order.containerPort }}
            initialDelaySeconds: {{ .Values.services.order.probes.readinessInitialDelaySeconds }}
            periodSeconds: {{ .Values.services.order.probes.readinessPeriodSeconds }}
          livenessProbe:
            httpGet:
              path: {{ .Values.services.order.probes.path }}
              port: {{ .Values.services.order.containerPort }}
            initialDelaySeconds: {{ .Values.services.order.probes.livenessInitialDelaySeconds }}
            periodSeconds: {{ .Values.services.order.probes.livenessPeriodSeconds }}
      volumes:
        - name: jwt-keys
          secret:
            secretName: {{ .Values.secrets.jwtKeysSecretName }}
{{- end }}
